# =============================================================================
# VendNet CI Pipeline (LEGACY — superseded by ci-security.yml)
# 
# This workflow is kept for reference. The full security pipeline with
# SAST, SCA, DAST, IAST, artifact scanning, and all OWASP Top 10 checks
# is in .github/workflows/ci-security.yml
#
# To migrate: rename or remove this file and ensure ci-security.yml is active.
# =============================================================================

name: CI Pipeline (Legacy)

on:
  workflow_dispatch:  # Manual only — disabled for push/PR triggers

jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::notice::This legacy pipeline is superseded by ci-security.yml"
          echo "::notice::Use the full CI / Security Pipeline for all checks."
